package TlsBuffIds
import ObjectIdGenerator

// ============================================================================
public class TlsBuffIds
  static constant int spikeSlow = compiletime(BUFF_ID_GEN.next())
  static constant int starving = compiletime(BUFF_ID_GEN.next())
  static constant int hungry = compiletime(BUFF_ID_GEN.next())
  static constant int fed = compiletime(BUFF_ID_GEN.next())
  static constant int wellfed = compiletime(BUFF_ID_GEN.next())