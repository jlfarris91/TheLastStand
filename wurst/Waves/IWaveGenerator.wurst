package IWaveGenerator
import WaveCollection
import NightWave
import DayWave

// ============================================================================
public interface INightWaveGenerator
  function generateWavesForPlayer(player targetPlayer, WaveCollection<NightWave> waves)

// ============================================================================
public interface IDayWaveGenerator
  function generateWavesForPlayer(player targetPlayer, WaveCollection<DayWave> waves)
