package HeroAge_config

// ============================================================================
@configurable public function shouldCountHeroForHeroAge(unit hero) returns bool
  return IsHeroUnitId(hero.getTypeId()) and hero.getOwner().isIngame()