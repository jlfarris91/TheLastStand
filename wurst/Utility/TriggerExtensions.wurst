package TriggerExtensions
import GameConstants

public function trigger.registerAnyPlayerChatEvent(string chatMessageToDetect, bool exactMatchOnly)
  for i = 0 to MAX_NUMBER_OF_PLAYERS - 1
    this.registerPlayerChatEvent(Player(i), chatMessageToDetect, exactMatchOnly)

// public function trigger.registerAnyPlayerLeaveEvent(playerevent e, code callback)
//   for i = 0 to MAX_NUMBER_OF_PLAYERS - 1
//     this.register (//(Player(i), chatMessageToDetect, exactMatchOnly)