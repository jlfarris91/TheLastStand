package ErrorIf
import ErrorHandling

public class ErrorIf

  @inline
  static function argumentIsNull<TArg>(TArg arg, string name)
    if (arg == null)
      error("Argument '{0}' cannot be null".format(name))