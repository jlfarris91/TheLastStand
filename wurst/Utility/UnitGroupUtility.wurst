package UnitGroupUtility
import GroupUtils

public function group.enumUnitsAll()
  var tempGroup = getGroup()
  for i = 0 to bj_MAX_PLAYER_SLOTS - 1
    tempGroup.enumUnitsOfPlayer(players[i], null)
    tempGroup.addGroup(this) // This is backwards
  tempGroup.release()