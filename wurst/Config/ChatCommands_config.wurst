package ChatCommands_config
import DisplayTextToPlayer
import initlater Console

// ============================================================================
@config public function chatCommandPrintToPlayer(player printTo, string msg)
  if (not Console.getIsReady())
    return
  if (not Console.getIsVisible())
    displayMessageToPlayer(printTo, msg)
  Console.addText(msg)